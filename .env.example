PORT=8080
HOST=0.0.0.0

# Minecraft connection
MC_HOST=127.0.0.1
MC_PORT=25565
MC_USERNAME=clawcraft
# "offline" (dev) or "microsoft" (real account)
MC_AUTH=offline
# Optional: set only for microsoft auth (device code flow is default if omitted)
MC_PASSWORD=

# Prismarine viewer
VIEWER_PORT=3000
VIEWER_FIRST_PERSON=false
VIEWER_VIEW_DISTANCE_CHUNKS=8

# Supervisor (AI)
SUPERVISOR_AUTOSTART=false
AI_GATEWAY_API_KEY=
AI_MODEL=openai/gpt-5.2
AI_REASONING_EFFORT=high

# Anthropic direct provider (supervisor prompt caching — 90% off cached tokens)
ANTHROPIC_API_KEY=
SUPERVISOR_MODEL=claude-opus-4-6
# "anthropic" = direct Anthropic with prompt caching, "gateway" = use AI_MODEL via gateway
SUPERVISOR_PROVIDER=anthropic

# Event log
EVENTS_JSONL_PATH=.data/events.jsonl
ASSETS_DIR=.data/assets

